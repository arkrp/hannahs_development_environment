custom_development_environment (Name pending)
Hannah Nelson 2026

TODO:
configure the ssh so that the sandbox key only has ftp access.
configure the host keys to work well.
test sshfs on the containers
make the core sandbox command
